#pragma once
#include <iostream>
#include <limits>
#include <string>

namespace in {
/**
@brief Takes an integer input and stores it by reference.
*/
void take_int_input(int &input, const std::string &message);
} // namespace in
